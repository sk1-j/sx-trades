export function tryParseJson(maybeJson) {
    try {
        return {
            result: JSON.parse(maybeJson),
            valid: true,
        };
    }
    catch (e) {
        return {
            result: undefined,
            valid: false,
        };
    }
}
//# sourceMappingURL=misc.js.map